/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  // This helps suppress hydration errors in production
  // suppressHydrationWarning: true, // Removed as it's not supported in Next.js 15

};

export default nextConfig;
